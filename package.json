{
  "name": "ep-studio-poc",
  "version": "1.0.0",
  "description": "Proof-of-concept server to test real image loading and Claude API cost outside the artifact sandbox.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "migrate": "node db/migrate.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
